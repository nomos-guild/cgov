# Backend API (server-side only)
BACKEND_API_URL=http://localhost:3001
BACKEND_API_KEY=

# Public site URL — used for canonical/OG/sitemap URLs (no trailing slash)
NEXT_PUBLIC_SITE_URL=https://app.cgov.io

# DeepL Translation API (server-side only)
DEEPL_API_KEY=

# Koios Cardano API (server-side only)
KOIOS_API_KEY=
