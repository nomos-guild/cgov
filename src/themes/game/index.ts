import type { ThemeDefinition } from "../types";
import components from "./components";

export const gameTheme: ThemeDefinition = {
  id: "game",
  label: "Chill Dark",
  isDark: true,
  components,
};

