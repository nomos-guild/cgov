import type { ThemeDefinition } from "../types";
import neuralComponents from "./components";

export const neuralTheme: ThemeDefinition = {
  id: "neural",
  label: "Neural Spike",
  isDark: false,
  components: neuralComponents,
};
